---
description: "Render an HTML file to PNG via Playwright. Usage: /render-html [path] [width] [height]"
disable-model-invocation: true
---

Invoke the canvas-design:render-engine skill. Navigate to the specified HTML file using Playwright, resize to the given dimensions, and take a screenshot. If no dimensions given, auto-detect from the HTML.
